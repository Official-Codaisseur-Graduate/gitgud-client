

export const scoreCalculator = (commitScore, branchScore, reproDescription, reproReadme) => {
  console.log(commitScore, 'commitScore', branchScore,'branchscor', reproDescription,'reproDesccr', reproReadme)

  // const totalCommitScore = Object.values(commitScore)
  
  // totalCommitScore.reduce(
  //   (partial_sum, a) => partial_sum + a
  // );

  // console.log(totalCommitScore, 'im total commitscore')


  

}

